# Ghost_FileShuttler
A production-ready, containerized local-network file-sharing system.

## Setup
1. Clone the repository.
2. Run `docker-compose up -d --build`
3. Access `https://localhost:8443` (or your LAN IP) and use the system!
